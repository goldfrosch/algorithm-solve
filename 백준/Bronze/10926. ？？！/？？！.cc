#include <iostream>

using namespace std;

int main() {
    // https://www.acmicpc.net/problem/10926
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    string N;
    std::cin >> N;

    printf("%s??!", N.c_str());

    return 0;
}